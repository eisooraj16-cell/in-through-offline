# in-through-offline
# Unzip first
unzip in-through-offline-source.zip
cd in-through-offline-source

# Push to GitHub
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/eisooraj16-cell/in-through-offline.git
git branch -M main
git push -u origin main
